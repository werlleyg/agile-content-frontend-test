import styled from "@emotion/styled";

export const Main = styled.main`
  width: min(100% - 2rem, 81.5rem);
  margin: 2.5rem auto;
  display: flex;
  gap: 2rem;
`;

export const Deck = styled.div`
  display: flex;
  flex-direction: column;
  gap: 2rem;
`;

export default Main;
